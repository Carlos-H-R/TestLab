#include <stdio.h>

int main()
{
	int vetot[5];
	int i, maior;
	for(i=0,i<5,i++)
	{
		printf("Insira o número: ");
		scanf("%d",&vetor[i])
	}
	maior = vetor[0];
	for(i=1,i<5,i++)
	{
		if(vetor[i]>maior)
		{
			maior = vetor[i];
		}
	}
	printf("O maior valor da lista é %d"maior)
}
